# XDVPN v1.5.10

## 更新了什么

- 修复隧道异常退出（崩溃或被强杀）后全局 DNS 残留的问题。过去这会让本机无法解析任何域名，等于断网，而自动重连又需要域名解析，形成死锁；只有重启才能恢复。
- 断开与清理不再依赖 `/tmp` 下的临时记录。`/tmp` 会被系统每天清理一次，长时间保持连接后这些记录可能已经消失，过去会导致清理漏项：残留的 openconnect 进程、关不掉的隧道、恢复不了的系统代理。现在这些对象都按自身的固定特征识别。
- 系统代理的原始开关状态改存到不会被系统清理的位置。这是唯一一项丢失后无法恢复的记录——一旦丢失，被 XDVPN 关掉的代理就再也开不回去。
- 修复清理失败时仍继续自动重连的问题；清理未确认成功时不再把新隧道建在状态不明的系统上。
- 修复休眠时若正在连接，清理动作可能长时间占用主线程的问题。

## 升级说明

本版更新了系统组件（helper）。首次启动后如果菜单栏提示需要配置，请点一次「一键配置」完成升级。

## 安装

1. 下载下面的 `XDVPN-v1.5.10.zip` 并解压。
2. 把 `XDVPN.app` 拖进 `/Applications/`。
3. 双击 `/Applications/XDVPN.app`；如果被 Gatekeeper 拦截，在“系统设置 → 隐私与安全性”中选择“仍要打开”。
4. 菜单栏出现锁盾图标后，首次使用请点“一键配置”安装系统组件。

## OpenConnect

OpenConnect 9.21 已随 XDVPN.app 内置，用户机器不需要预装 Homebrew 或 OpenConnect。

## 说明

- 未经 Apple 公证：个人自用项目，没有 Apple Developer 账号。源码公开，可自行运行 `./build.sh` 从头构建。
- 支持 macOS 14+。
